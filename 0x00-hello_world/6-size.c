#include <stdio.h>
/**
 * main - Entry point
 *
 *Descreption : a C program that prints the size of various types 
 *
 * Return: always 0 (success)
 */
int main(void){
	
	char s;
	int b;
	long int l;
	long long int c;
	float z;


	printf("The size of char: %lu bytes (s) \n", sizeof(s);
	printf("The size of int: %lu bytes (s) \n", sizeof(b);
	printf("The size of long int: %lu bytes (s) \n", sizeof(l);
	printf("The size of long long int: %lu bytes (s) \n", sizeof(c);
	printf("The size of float: %lu bytes (s) \n", sizeof(z);

return (0);
}
