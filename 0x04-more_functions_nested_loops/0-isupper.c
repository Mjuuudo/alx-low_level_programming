#include "main.h"
/**
 * _isupper - i suppeur case or lower case
 * @c: number entred
 * Return: 1 or 0
 */
int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
